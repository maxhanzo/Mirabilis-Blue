//
//  BluetoothError+LocalizedError.swift
//  Mirabilis Blue
//
//  Created by Max Ueda on 31/08/26.
//
/*
import Foundation

extension BluetoothError: LocalizedError {

    var errorDescription: String? {
        switch self {

        case .bluetoothUnavailable(let state):
            return "Bluetooth is unavailable: \(state)."

        case .deviceNotFound:
            return "The Bluetooth device could not be found."

        case .serviceNotFound:
            return "The requested Bluetooth service was not found."

        case .characteristicNotFound:
            return "The requested characteristic was not found."

        case .connectionFailed(_, let reason):
            return reason ?? "Bluetooth connection failed."

        case .disconnected(_, let reason):
            return reason ?? "The Bluetooth device disconnected."

        case .serviceDiscoveryFailed(let reason):
            return reason

        case .characteristicDiscoveryFailed(let reason):
            return reason

        case .readFailed(_, let reason):
            return reason

        case .writeFailed(_, let reason):
            return reason

        case .notificationFailed(_, let reason):
            return reason

        case .unsupportedOperation:
            return "The requested GATT operation is not supported."

        case .securityRequired:
            return "This operation requires an encrypted connection."

        case .securityStateMismatch:
            return "The Bluetooth security state must be re-established."
        }
    }
}
*/
